A simple currency converter written with JavaScript, HTML, and CSS. 

Currently the code allows the user to convert the base GBP currency to any other currency with their respective conversion rates.
